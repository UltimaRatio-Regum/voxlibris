I'd like to setup something like dependency injection for the TTS APIs.  Rather than the tool needing to have custom code for each different TTS API, for a new TTS engine, I should be able to specify a URL and optional API key (for Bearer style auth/authz) in the settings tab, and then on startup, for each TTS API, the VoxLibris app (this app) will query the TTS API (with the URL as the root endpoint of the REST handler on the engine's side, and the postgres db should store the URL/optional API key for each engine) using a GET request to a method named GetEngineDetails (that takes no parameters) to get details on capabilities (and also to validate the URL/engine can be reached and called, in case an engine can't be reached, in which case, if the GetEngineDetails times out, or there's a DNS or HTTP(S) error message, the app should temporarily hide that engine from the dropdown list of engine options when running a job and display the error message in or below the row for the engine in the engine list gridview on the settings tab, and refresh the engine detail cache (in postgres) with updated values for:
1) suggested maximum length (number of seconds) per request (usually not a hard limit, but it allows us to roughly estimate number of words per chunk)
2) return format (should always be a pcm-encoded .wav file), in terms of sample rate/bit depth/number of channels to expect
3) Name of the TTS engine in the dropdown (how it should be displayed), and engine_id (how the app will determine if, when adding a new engine to the list, if one already exists for this engine), e.g. engine_name might be "Chatterbox TTS" and engine_id = "chatterbox-tts"
4) Voice handling: whether or not voice cloning is supported, and if there is a "built-in" voice library to choose from, for each voice, the engine should return a dictionary with keys "id" (some sort of unique identifier for each voice that gets passed to the engine), "display_name" (displayed to the user when picking a voice), and optionally, "extra_info", just a description string that provides more details about the voice, and "voice_sample_url", if available, a full URL that will return a short sample of the voice from the library, like a 10 second clip or something similar (this can be null if the engine doesn't support this, however since we'll be writing the wrapper interface for each engine, most engines should have output samples available for each built-in voice (this will be up to each engine to provide).

Please specify a JSON schema to use to hold the above information along with the endpoint function name that VoxLibris will expect (GetEngineDetails specifically) to exist in order to get engine details (we may add more engine parameters to return as the number/type of engines increases, but the JSON schema should only ever be appended to, so something like a name/value pair list of properties (like a dictionary with the keys EngineName, SampleRate, MaxSecondsPerConversion, BitDepth, VoiceCloningSupported, BuiltinVoiceList for now, but as capabilities expand, there may be more options added) is probably sufficient, and the type of the value can be elementary or complex. For BuiltinVoiceList, it should return an array where each element is a JSON object with three string properties: id, display_name, extra_info. If it doesn't have any voices built-in, then it can just return an empty array, however either VoiceCloningSupported must be true *or* there must be at least one entry in the BuiltinVoiceList

Then, the documentation should detail the interface for the method that is called, using a POST request called ConvertTextToSpeech that accepts as input:

1) voice details, which can either be the contets of a pcm-encoded .wav file (like a blob) in a parameter named something like voiceToCloneSample (and even if the engine doesn't support voice cloning, for the purposes of defining an interface, the TTS engine should include this parameter, but simply ignore it, and if there's a built-in voice library, the id of the voice in the parameter builtinVoiceId (which can again be ignored if the engine does not have a built-in voice list.  If both values are null, the engine should fall back to a default voice
2) randomSeed, just an int that can be passed to the underlying engine (if the engine accepts a random seed), or a null/empty value to have the engine randomly choose
3) inputText, the string of text to convert
4) emotionSet, an array of emotion names where the first element is the dominant emotion, and then each value afterwards represents a modification of the main emotion (each element has to be chosen from the fixed list that's used to assign the one or more emotional dimensions to the chunk; if no emotions are passed, then the engine should default to a "neutral" tone
5) intensity, volume (each ranging from 1 - 100, can be scaled to whatever the underlying TTS engine expects from inside the implementation of the interface for that engine), representing the level of emotional intensity and volume of the chunk
6) speedAdjust, pitchAdjust: each ranging from -5.0 to 5.0, representing a percent adjustment in speed (so up to 5% slower to 5% faster), and a percent adjustment in pitch, again from 5% lower pitch to 5% higher pitch.  Depending on how the engine handles emotion, these values and the intensity value may be ignored if the engine has more complex emotion handling (like the ability to specify a named emotion as part of the input to the engine, which then makes much more complicated adjustments to how the speech sounds).

So, please use the above information to generate a markdown file containing details of the TTS API interface/contract so that it can be used to actually create implementations for different engines.

Additionally, add a feature in the VoxLibris settings tab that allows us to maintain the engine list: to add an engine, just enter in the URL and optionally an API key for the engine (which should use the Bearer header for auth/authz), hit an "add engine" button, this will then query the engine details from the URL provided and store/cache the engine details (including URL and API key) in postgres.  Then the settings tab should list all available engines with the engine name (as returned from the GetEngineDetails call), the URL, and the API key.  Should the URL or API key need to be changed, the simplest thing is just to have an option to delete the engine (so this list would be like a grid view with a delete button next to each row, but none of the values would be editable, so the user can delete and re-add if the URL or key changes).  If a user adds an engine, if the URL being added already exists, update the API key (if provided), and in all cases (new engine or update), call GetEngineDetails and refresh any cached engine options the app has stored about the engine.  If upon calling GetEngineDetails, the engine's engine_id is already in the list, prompt the user to choose whether to overwrite the base URL and API key with the new versions or to keep the existing URL/API key/cached engine details.

Finally, instead of having the user upload a voice sample each time they want to clone a voice, the set of clonable voices should be stored in postgres. the current drizzle schema has a type called libraryVoiceSchema; this is exported as the type LibraryVoice, and this should be used as the schema for a new table in postgres, with one tweak: instead of storing audioUrl, we actually store the sample blob containing the sample's pcm .wav file contents. Then, the settings tab should have a gridview listing all voice samples currently available, along with options to delete voice sample rows, edit the metadata associated with the sample, etc., and the ability to upload a new sample, along with its metadata, such as the voice's name, description, gender, age, location of speaker, voice sample .wav, transcript of voice sample .wav (optional), etc.  In order to do a refresh/one-time load of the voices from the voice_samples/ folder into postgres, there should also be a python script or node.js script that can be run to ensure this table exists and to reload it with all the data from voice_samples (it can use the same method we're currently using to parse the file names from within the voice_samoples folder to get things like age, gender, location fo speaker, etc.  Once this is run once, then it would only need to be re-run if the DB schema changes, or the tables need to be recreated.

So, quick recap:
1) Create markdown doc describing the structure of the TTS API so that I can hand it to other tools/devs to implement it for various engines -- DO THIS FIRST (this way I can provide the document to other tools that will be creating the API endpoints for various engines within huggingface spaces)
2) Update the app so that it uses this new REST-based API definition for all TTS operations, meaning for the engine-specific voice selection (for engines with built-in voices), and for from within the methods that actually do the TTS jobs
3) Add the TTS engine management section in the settings tab (where you pass in an engine URL (and key if necessary), and it then uses the URL to get the engine details, stores those in postgres, and then shows the new engine in the engine list
4) Store clonable voice samples and metadata in postgres (for use with engines that support voice cloning)
5) Add the voice sample management interface to settings
6) Add the script to initialize the voice library by loading all the samples from voice_samples/ into the postgres table(s) associated with the LibraryVoice type


